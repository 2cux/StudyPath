# java项目前置知识的学习

## 一、MySQL数据库

- 约束

> 1.外键的主要作用就是连接其他表的主键，使表之间产生关系（产生约束），这样当操作不当(在订单表添加一个不存在的的用户)时，数据库会阻止操作
>
> 2.主键、核心业务字段、有默认值的字段都要是非空的(NOT NULL)

- 事务

> 简单来说，事务就是确保操作正常运行的一个规则，确保一组操作要么全成，要么全废

- 锁机制

> 锁就是实现事务的一种机制之一，通过锁定资源，使对资源的操作能够正常进行，但是也会出现死锁的情况，也就是事务的两端都上锁了导致操作不能正常进行，这时就需要逻辑上调整锁的顺序了

- 设计数据库表(LucidChart)

> 1.尽可能详细地写出需求
>
> 2.找出需求中的名词，一个名词代表一个表格(table)
>
> 3.为表格添加属性，也就是字段(field)
>
> 4.评估表格和字段，做出相应的调整

在设计数据库表的时候需要注意的事项

> 1.要给常用查询字段添加索引(index)，这样可以加快查询速度和优化数据写入速度
>
> 2.避免冗余的字段，比如有了birth-day就完全没有必要再加一个age字段
>
> 3.表名用驼峰命名法或者下划线，单词之间不要有空格

## 二、java web

- XML

> XML是一种能跨平台的纯文本标签语言，核心作用是存储和运输数据，可根据需求自定义标签，在java开发中常用作框架/服务器的配置文件

- YAML

> 相比于XML，YAML因其简洁性在现代开发中更加常见，其作用也是存储和运输数据，但是其语法主要是缩进和键值对，是Springboot开发框架核心配置文件的首选

- JSON

> JSON基于 JavaScrip t语法，是一种文本数据交换格式，核心作用也是存储和运输数据，相比于YML，它能被机器更高效地解析，因此常用于前后端数据的交换

- Servlet

> 简单来说，Servlet 是服务器的一个组件，相当于客户端和服务器中间的中介，Servlet接收客户端传来的请求，处理请求，然后返回响应给客户端

> 一个服务器可以有多个 Servlet ，服务器中的一个web应用也可以有多个 Servlet ，但是一个 Servlet 只能负责一个业务板块，这个Servlet可以进行多线程任务，也就是处理多个用户

> 现代 SpringMVC 开发中，原生 Servlet 不再由开发者手写，框架将 Servlet 的核心功能（请求接收、处理、响应）封装成了  @Controller 注解的组件，开发者编写的业务请求处理代码，都集中在 Controller 层（替代了传统手写 Servlet 的工作）

- Filter(过滤器)

> 作为Servlet的前置/后置拦截器，拦截前端传来的请求和 Servlet 传出的响应，相当于把多个 Servlet 的通用处理提取出来单独做成一个组件，提升效率

- Listener(监听器)

> 被动触发，只有碰到特定的时间时，才会触发监听器，比如应用启动时， listener 就会监听到，就开始加载配置文件等

- Cookie

> 简单来说，Cookie 就是 web 网页保存在浏览器的数据，这样，在你每次访问网页的时候，浏览器就会出示  Cookie ，证明你的身份，让你不用重复登入

- Session(会话)

> 用户使用网页的过程就是一段会话，这段会话会创建一个随机的 Session ID ，其会作为 Cookie 的核心组成部分传递给客户端的浏览器

## 三、Spring MVC

> 一句话，就是java轻量级web开发框架，里面实现了结构分层，使开发过程更简便高效

## 四、代码规范

1. 每行一条语句
2. 换行后新行至少缩进4个空格
3. 类成员之间、方法之间、逻辑分组需要空行
4. 包名全部小写,类名使用大驼峰命名法，方法名、变量名使用小驼峰命名法，常量名使用大写下划线命名法
5. 抛出的异常要记录下来
6. 所有 public 类和方法上面都要加javadoc,也就是简短介绍一下功能
7. @param 标签用于描述方法的参数名和参数说明
8. @return 标签用于描述返回值

## 五、设计模式

> 简单来说，设计模式是有经验的人总结出的一些好用的套路，遇到类似问题时可以直接套用

以下是几种常见的设计模式：

1. 单例模式（Singleton Pattern）

   > 保证一个类在程序中只能有一个对象，并且所有地方都用这同一个对象，从而节省资源、保证数据一致性，并方便统一管理系统级对象。
   >
   > 基本步骤就是：构造函数私有化、保存唯一的单例对象、提供统一访问入口

   > Springboot的IOC默认就是单例模式
   
   > 注意，单例模式只适合在全局唯一资源是使用，比如：日志、线程池/连接池、缓存组件等，大部分对象其实都不是单例
   
2. 原型模式（Prototype Pattern）

   > 就是事先准备一个样板对象，以后需要新对象是不需要new，直接照模板复制一份，这样创建对象更快，而且不用初始化一堆复杂参数。
   >
   > 基本步骤就是：定义一个类让它实现Cloneable，重写clone方法

   > 在样板对象里面还有对象的情况下，还分为浅克隆和深克隆
   
   > 在Spring Boot中，给原型类加上@Scope("prototype")，就表示这个是个原型类，不用继承Cloneable，这个是每次new一个对象，不设计浅拷贝、深拷贝
   
3. 工厂模式（Factory Pattern）

   > 简单来说就是把创建对象的这一过程封装成工厂类，其专门负责生产对象，这样不用在主程序入口写创建对象的代码，降低了耦合，方便后续添加新对象和维护代码解
   >
   > 基本步骤就是：定义接口、实现接口、创建工厂类

   > 在Spring Boot中，工厂模式更容易实现，因为IOC（依赖注入），本身就是一个经过封装的超级厂
   
   补充知识：将创建的Bean注入Map中，可以通过键值对快速找到类
   
4. 抽象工厂模式（Abstract Factory Pattern）

   > 抽象工厂模式简单来说就是对工厂模式的再升级（再封装），工厂模式只能创建一类产品，而抽象工厂模式可以创建一系列产品族

   > 在Spring Boot环境下，对象的创建主要又Spring容器完成，其本质上就是，抽象工厂模式 + 单例模式的组合

   补充知识：可以将抽象工厂的实例化对象注入Map中，这样子就实现了解耦
   
5. 观察者模式（Observer Pattern）

   > 一句话解释就是：一个对象发生变化，自动通知一群关心它的对象，这样实现了解耦，不用把所有的代码写在一起
   >
   > 